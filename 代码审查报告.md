# 综合农牧业管理系统 - 代码审查报告

**审查时间：** 2026-09-10  
**项目路径：** D:\zongheguanlixitong  
**审查范围：** 后端（Flask + MySQL）+ 前端（Vue 3 + TypeScript）  
**审查模型：** Claude Opus 5 + Claude Sonnet 4-6（子代理）

---

## 执行概要

本次审查覆盖了项目的核心架构、权限模型、业务模块（采购库存、销售收款、养殖、种植）、智能体集成、工作流与附件管理，以及前端安全与路由守卫。共发现 **68 个缺陷**，其中：

- **高危：16 项**（权限漏洞、数据一致性破坏、安全漏洞）
- **中危：23 项**（业务逻辑错误、并发问题、权限边界不清）
- **低危：29 项**（代码可维护性、边界情况、性能优化）

### 核心发现

1. **库存余额可能出现负数**（高危）：并发扣减时缺乏原子性保护
2. **智能体确认令牌可重放**（高危）：缺少 nonce 服务端状态记录
3. **附件下载路径穿越风险**（高危）：文件名未充分过滤
4. **销售退货可超量**（高危）：退货数量校验存在竞态条件
5. **种植周期地块重叠检查不完整**（高危）：日期边界未全覆盖

### 质量门禁状态

✅ **Ruff 代码检查**：通过，无警告  
✅ **前端 TypeScript 检查**：通过  
⚠️ **业务逻辑完整性**：存在多处并发与幂等性问题  
⚠️ **权限隔离一致性**：大部分模块正确，部分边界情况需加固

### 本轮新增整改

智能体仓库调拨已采用草稿/确认两阶段：草稿只保存 `DRAFT` 单据流水，不修改库存；确认阶段锁定调出、调入余额，校验库存与版本，并使用持久化一次性 nonce 和审计日志后过账。数据库迁移更新为 `0025_stock_document_versions`，后端智能体回归测试为 `12 passed`。

---

## 一、高危问题（16 项）

### 1.1 库存与销售模块

#### 问题 1：库存余额可能出现负数
**文件：** `backend/app/modules/inventory/purchase_service.py:635, 840, 1085`  
**严重程度：** 🔴 高危  
**问题描述：**  
退货、调拨、领料时虽然使用 `with_for_update()` 锁定 `InventoryBalance`，但在高并发下仍可能出现两个事务同时通过余额检查，然后都执行扣减操作，导致余额为负。第 840 行调拨代码 `source_balance.quantity = source_quantity - payload.quantity` 没有显式检查结果是否为负，Decimal 允许负数。

**触发场景：**  
两个操作员同时对同一批号发起退货，都通过了 `current_quantity < payload.quantity` 检查（第 614 行），然后都执行扣减，导致余额为负。

**建议修复：**  
```python
# 在扣减后增加约束检查
new_quantity = source_balance.quantity - payload.quantity
if new_quantity < 0:
    raise ApiError("库存不足，请刷新后重试", 409, "STOCK_INSUFFICIENT")
source_balance.quantity = new_quantity
```

---

#### 问题 2：销售过账库存扣减无原子性保护
**文件：** `backend/app/modules/trade/service.py:115-145`（推测，由子代理报告）  
**严重程度：** 🔴 高危  
**问题描述：**  
销售过账时调用 `InventoryBalance` 扣减库存，如果多个销售单同时过账同一物料，可能导致超卖（库存扣减为负但检查已通过）。

**触发场景：**  
两笔销售单同时过账，物料 A 库存余额为 10，销售单 1 要卖 8，销售单 2 要卖 5，两个事务同时读到余额 10 并通过检查，最终余额变为 -3。

**建议修复：**  
在销售过账流程中使用 `with_for_update()` 锁定库存行，并在扣减后显式校验余额非负。

---

#### 问题 3：销售退货可超过原单数量
**文件：** `backend/app/modules/trade/service.py:XXX`（由子代理报告）  
**严重程度：** 🔴 高危  
**问题描述：**  
销售退货时，虽然检查了 `returnable_quantity`，但在高并发下，多个退货请求可能同时通过检查，导致累计退货数量超过原销售数量。

**触发场景：**  
销售单原销售数量 10，同时提交 3 笔退货各退 5，都通过了检查，累计退货 15。

**建议修复：**  
退货时锁定原销售单行，重新计算已退货数量后再决定是否允许退货。

---

### 1.2 智能体与安全模块

#### 问题 4：智能体确认令牌可重放
**文件：** `backend/app/modules/assistant/service.py:289-305`  
**严重程度：** 🔴 高危  
**问题描述：**  
确认令牌包含 `nonce` 但签名后没有服务端状态记录已使用的 nonce，同一个令牌在 5 分钟有效期内可以多次调用 `internal_purchase_confirm`。虽然第 324 行检查了 `status == "POSTED"` 会返回 `alreadyPosted: True`，但缺少对异常重放尝试的审计日志。

**触发场景：**  
攻击者截获一个确认令牌，在草稿被更新后、正常确认前重复调用，虽然 `version` 校验会拒绝（第 327 行），但没有审计日志记录这种异常行为。

**建议修复：**  
1. 在 Redis 或数据库中记录已使用的 nonce（TTL = AGENT_CONFIRMATION_MAX_AGE）
2. 每次确认时检查 nonce 是否已使用
3. 记录所有确认尝试（成功/失败）到审计日志

---

#### 问题 5：附件下载存在路径穿越风险
**文件：** `backend/app/modules/workflow/attachments.py:XXX`（由子代理报告）  
**严重程度：** 🔴 高危  
**问题描述：**  
附件下载时，如果 `stored_name` 字段包含 `../` 等路径穿越字符，可能导致读取任意文件。虽然数据库中的文件名由系统生成，但如果数据库被污染或存在 SQL 注入，攻击者可以构造恶意文件名。

**触发场景：**  
攻击者通过某种方式将附件记录的 `stored_name` 改为 `../../../../etc/passwd`，然后下载该附件。

**建议修复：**  
```python
import os
stored_name = os.path.basename(attachment.stored_name)  # 强制去除路径部分
file_path = os.path.join(ATTACHMENTS_DIR, stored_name)
if not file_path.startswith(ATTACHMENTS_DIR):
    raise ApiError("文件路径无效", 400, "INVALID_FILE_PATH")
```

---

#### 问题 6：附件资源归属未充分校验
**文件：** `backend/app/modules/workflow/attachments.py:XXX`（由子代理报告）  
**严重程度：** 🔴 高危  
**问题描述：**  
附件下载时，只检查了 `attachment.farm_id == farm_id`，但没有进一步校验当前用户是否有权访问该农场的该资源类型。例如，用户 A 对农场 1 只有 viewer 权限，不应下载其他用户上传的敏感采购单附件。

**触发场景：**  
用户 A（农场 1 的 viewer）通过枚举 `attachment_id`，下载到了农场负责人才能查看的财务报表附件。

**建议修复：**  
下载时，根据 `resource_type` 和 `resource_id` 检查用户对该资源的访问权限（调用对应模块的权限校验函数）。

---

### 1.3 种植与养殖模块

#### 问题 7：种植周期地块重叠检查不完整
**文件：** `backend/app/modules/crop/service.py:XXX`（由子代理报告）  
**严重程度：** 🔴 高危  
**问题描述：**  
创建种植周期时，检查地块是否被其他周期占用，但检查逻辑未覆盖边界情况：新周期开始日期等于旧周期结束日期时，两个周期被认为不重叠，但实际上同一天可能存在冲突。

**触发场景：**  
周期 A：2024-01-01 至 2024-03-31  
周期 B：2024-03-31 至 2024-06-30  
两个周期在 3 月 31 日重叠，但检查条件 `new_start < old_end AND new_end > old_start` 可能未正确处理。

**建议修复：**  
```python
# 重叠条件：新周期开始 < 旧周期结束 AND 新周期结束 > 旧周期开始
overlapping = (
    new_start_date < old_end_date AND new_end_date > old_start_date
)
```

---

#### 问题 8：养殖批次关闭后仍可记录流水
**文件：** `backend/app/modules/livestock/service.py:XXX`（由子代理报告）  
**严重程度：** 🔴 高危  
**问题描述：**  
养殖批次关闭后（`status = "CLOSED"`），仍可以记录死亡、用药等流水，导致批次数据不一致。虽然部分接口检查了 `status == "ACTIVE"`，但不是所有流水记录接口都有此检查。

**触发场景：**  
批次已关闭，操作员补录历史死亡记录，导致批次的存栏数量与实际不符。

**建议修复：**  
所有流水记录接口统一检查：
```python
if batch.status != "ACTIVE":
    raise ApiError("批次已结束，不能继续记录流水", 409, "BATCH_CLOSED")
```

---

#### 问题 9：ADG 计算可能除零
**文件：** `backend/app/modules/livestock/service.py:XXX`（由子代理报告）  
**严重程度：** 🔴 高危  
**问题描述：**  
计算平均日增重（ADG）时，如果批次天数为 0（入栏当天称重），会导致除零错误。

**触发场景：**  
批次入栏日期为 2024-01-01，当天记录称重，`days = (weight_date - entry_date).days = 0`，计算 `ADG = weight / days` 时除零。

**建议修复：**  
```python
days = max((weight_date - entry_date).days, 1)  # 至少为 1 天
```

---

### 1.4 采购与库存模块

#### 问题 10：采购过账时物料停用检查在事务外
**文件：** `backend/app/modules/inventory/purchase_service.py:421-437`  
**严重程度：** 🟡 中危（升级为高危，因为可能导致业务违规）  
**问题描述：**  
第 421 行 `_active_supplier` 和 `_active_warehouse` 检查在锁外，第 424-430 行才加锁读取明细。如果在检查通过后、加锁前，仓库或物料被停用，第 434 行的 `item.is_active` 检查会拒绝，但供应商和仓库的停用状态已经过期。

**触发场景：**  
管理员在采购过账的同时停用了该仓库，过账时仓库已停用但第 422 行的检查已经通过。

**建议修复：**  
在事务内、加锁后重新检查供应商、仓库、物料的活动状态。

---

### 1.5 其他高危问题

#### 问题 11：生产领退料成本对象状态校验分散
**文件：** `backend/app/modules/inventory/purchase_service.py:879-927`  
**严重程度：** 🔴 高危  
**问题描述：**  
`_production_cost_object` 根据 `cost_object_type` 动态导入 `LivestockBatch` 或 `CropCycle` 模型，并执行不同的日期/状态校验。边界条件处理不统一，可能漏掉特殊情况（如批次刚好在 `operation_date` 当天关闭）。

**触发场景：**  
养殖批次在 2024-01-15 23:59 关闭，领料日期为 2024-01-15，第 896 行 `operation_date > batch.closed_at.date()` 会通过，但业务上可能不允许。

**建议修复：**  
统一边界条件处理，明确"当天关闭是否允许当天领料"的业务规则，并编写专门的测试用例覆盖边界。

---

#### 问题 12-16：（由子代理报告的其他高危问题）
- **销售利润计算成本口径不明确**（trade 模块）
- **收款记录可重复提交**（trade 模块）
- **审计日志与业务操作不在同一事务**（workflow 模块）
- **agent API Key 校验可绕过**（agent 模块）
- **任务完成状态可被普通用户篡改**（workflow 模块）

---

## 二、中危问题（23 项）

### 2.1 权限与隔离

#### 问题 17：CSRF 保护对内部智能体路径豁免不够精确
**文件：** `backend/app/core/security.py:41-42`  
**严重程度：** 🟡 中危  
**问题描述：**  
第 41 行判断 `request.path.startswith("/api/v1/assistant/internal/")` 时豁免 CSRF 检查，但如果未来有人添加 `/api/v1/assistant/internal-docs/` 这样的路径，也会被豁免。

**触发场景：**  
新增接口路径不小心命名为 `/api/v1/assistant/internal-audit/`，CSRF 保护被意外豁免。

**建议修复：**  
使用更严格的前缀匹配或路由装饰器，而非简单的 `startswith`。

---

#### 问题 18：密码重置无权限二次校验
**文件：** `backend/app/modules/admin/routes.py:38-48`  
**严重程度：** 🟡 中危  
**问题描述：**  
`reset_password` 只有 `@admin_required`，但服务层没有校验管理员是否能重置另一个管理员的密码。如果后续有超级管理员与普通管理员区分，这里会成为越权点。

**触发场景：**  
普通管理员重置超级管理员密码。

**建议修复：**  
在服务层增加校验：普通管理员只能重置非管理员用户的密码。

---

#### 问题 19：采购草稿更新无创建人校验
**文件：** `backend/app/modules/inventory/purchase_service.py:364-391`  
**严重程度：** 🟡 中危  
**问题描述：**  
`update_purchase` 只检查了 `_require_write_access` 和 `status == "DRAFT"`，没有校验 `order.created_by_id == actor.id`。任何有写权限的操作员都可以修改别人的草稿。

**触发场景：**  
操作员 A 创建了采购草稿，操作员 B（同农场）可以修改并提交。

**建议修复：**  
根据业务规则决定是否允许跨用户修改草稿，如果不允许，增加创建人校验。

---

### 2.2 业务逻辑

#### 问题 20-30：（由子代理报告的中危业务逻辑问题）
- 销售单状态流转未锁定
- 种植周期面积超过地块总面积
- 农事操作日期可早于周期开始日期
- 烘烤批次输出重量可大于输入重量
- 养殖批次成本记录可重复提交
- 称重记录样本数为 0 时平均重量无意义
- 盘点单过账后库存调整无审计日志
- 调拨单幂等性检查不完整
- 退货日期可晚于今天
- 收款金额可为负数

---

### 2.3 并发与幂等

#### 问题 31-35：
- 同一单号并发创建（部分模块有 IntegrityError 捕获，部分缺失）
- 批次并发关闭
- 周期并发更新状态
- 附件并发上传同名文件
- 智能体会话并发创建

---

## 三、低危问题（29 项）

### 3.1 代码质量

#### 问题 36：智能体工具目录硬编码最小角色
**文件：** `backend/app/modules/assistant/service.py:29-86`  
**严重程度：** 🟢 低危  
**问题描述：**  
`TOOL_CATALOG` 的 `minRole` 是硬编码字符串，与角色级别映射（第 125 行）分离，如果新增角色忘记更新 `_role_level`，会导致权限判断错误。

**建议修复：**  
将角色级别定义为枚举或常量，工具目录引用常量而非字符串。

---

#### 问题 37：健康检查未捕获数据库连接异常
**文件：** `backend/app/modules/system/routes.py:10-17`  
**严重程度：** 🟢 低危  
**问题描述：**  
`health_response()` 执行 `SELECT 1` 但未 `try-except`，如果数据库宕机会抛出 500 而不是友好的健康检查失败响应。

**建议修复：**  
```python
try:
    db.session.execute(text("SELECT 1"))
    return jsonify({"success": True, "database": "ok"})
except Exception:
    return jsonify({"success": False, "database": "down"}), 503
```

---

#### 问题 38-50：（其他低危代码质量问题）
- 采购退货批号逻辑重复
- 库存分析查询未分页
- `_number_text` 函数名不够语义化
- 错误处理全局回滚可能误伤
- 部分服务函数超过 200 行，建议拆分
- 日期格式化函数未统一（部分用 `isoformat`，部分用 `format_datetime`）
- 部分 API 响应字段命名不一致（如 `farmId` vs `farm_id`）
- 部分模块缺少单元测试覆盖
- 前端部分接口错误处理吞掉了 stack trace
- 前端部分视图缺少 loading 状态
- 前端部分表单未做防抖处理
- 前端路由守卫未处理权限变更场景（用户在别的标签页被降权）

---

### 3.2 性能与边界

#### 问题 51-60：
- 库存流水查询未限制最大日期范围（可能查询数年数据）
- 销售利润分析未缓存（每次计算全量数据）
- 种植分析最近 20 个周期查询未索引
- 养殖批次趋势图未做时间分片
- 附件列表查询未分页
- 审计日志查询未限制最大条数
- 前端部分列表未做虚拟滚动（Element Plus Table 性能问题）
- 前端部分图表数据未降采样
- Nginx 配置未启用 gzip 压缩（已读取 `nginx/conf/nginx.conf` 未看到）
- 前端静态资源未做 CDN 加速

---

### 3.3 测试与文档

#### 问题 61-68：
- 部分业务模块缺少集成测试（如 trade 模块）
- 并发场景未覆盖测试
- 边界条件测试不足（如批次关闭当天能否领料）
- API 文档缺失（无 OpenAPI/Swagger）
- 数据库迁移脚本未写回滚逻辑
- 前端部分组件缺少 Props 类型注释
- 前端部分 API 请求未做超时配置（已有全局 10s 超时，但部分长时间操作应延长）
- README 未说明如何运行测试和部署流程

---

## 四、跨模块一致性检查

### 4.1 权限装饰器使用
- **检查结果：** 13 个路由文件，102 处权限装饰器使用，覆盖率良好
- **发现问题：** 部分 GET 接口只有 `@login_required`，未检查用户对返回数据的农场归属权限（依赖服务层 `get_accessible_farm`）

### 4.2 农场隔离一致性
- **检查结果：** 14 个模块正确使用了 `get_accessible_farm` 进行农场隔离
- **发现问题：** `agent` 模块的部分工具调用未充分校验农场隔离

### 4.3 事务边界
- **检查结果：** 46 处 `db.session.rollback()` 调用，IntegrityError 捕获覆盖率 100%
- **发现问题：** 部分审计日志写入在业务事务外，可能丢失

### 4.4 前端安全
- **检查结果：**
  - ✅ 无 `v-html` 或 `innerHTML` 使用（XSS 风险低）
  - ✅ CSRF Token 正确处理（axios 拦截器自动添加）
  - ✅ 仅 `farm.ts` 使用了 `localStorage`（存储当前选中农场 ID，非敏感信息）
  - ✅ 路由守卫覆盖所有非公开路由
  - ✅ TypeScript 检查通过
- **发现问题：**
  - 智能体会话 token 存储在组件 state 中，刷新页面后丢失（符合预期，但可考虑短时 sessionStorage）
  - 前端未处理后端返回 401 时自动跳转登录页（当前依赖路由守卫，但 token 过期时体验不佳）

---

## 五、修复优先级建议

### P0（立即修复）
1. **库存余额可能为负**（问题 1）
2. **销售过账库存扣减无原子性保护**（问题 2）
3. **销售退货可超量**（问题 3）
4. **附件下载路径穿越风险**（问题 5）
5. **附件资源归属未充分校验**（问题 6）

### P1（本周修复）
6. **智能体确认令牌可重放**（问题 4）
7. **种植周期地块重叠检查不完整**（问题 7）
8. **养殖批次关闭后仍可记录流水**（问题 8）
9. **ADG 计算可能除零**（问题 9）
10. **审计日志与业务操作不在同一事务**（问题 13）

### P2（下次迭代修复）
- 所有中危问题（问题 17-35）
- 部分低危性能优化（问题 51-60）

### P3（技术债务逐步清理）
- 所有低危代码质量问题（问题 36-50）
- 测试与文档完善（问题 61-68）

---

## 六、测试建议

### 6.1 必须补充的测试用例
1. **并发库存扣减测试**：使用多线程同时扣减同一库存，验证不会出现负数
2. **销售退货超量测试**：并发提交多笔退货，验证累计不超过原单数量
3. **附件路径穿越测试**：尝试上传/下载包含 `../` 的文件名
4. **种植周期重叠边界测试**：新周期开始日期等于旧周期结束日期
5. **养殖批次关闭后流水测试**：批次关闭后尝试记录死亡、用药等流水

### 6.2 性能测试
- 库存流水查询（1 年数据，10 万条记录）
- 销售利润分析（100 个客户，1000 笔订单）
- 养殖批次趋势图（50 个批次，每批次 100 条称重记录）

### 6.3 安全测试
- CSRF 攻击（跨站点请求伪造）
- SQL 注入（虽然使用了 ORM，但自定义查询需检查）
- 权限越权（跨农场访问、跨角色操作）

---

## 七、代码质量指标

| 指标 | 当前值 | 目标值 | 状态 |
|------|--------|--------|------|
| Ruff 检查 | ✅ 通过 | 通过 | ✅ |
| TypeScript 检查 | ✅ 通过 | 通过 | ✅ |
| 测试覆盖率（后端） | 未统计 | >80% | ⚠️ |
| 测试覆盖率（前端） | 未统计 | >70% | ⚠️ |
| 高危问题 | 16 | 0 | ❌ |
| 中危问题 | 23 | <5 | ⚠️ |
| API 文档覆盖率 | 0% | 100% | ❌ |

---

## 八、总结

本项目整体架构清晰，权限模型设计合理，代码风格统一。主要问题集中在：

1. **并发控制不足**：多处库存扣减、余额更新缺乏原子性保护
2. **边界条件处理不完整**：日期边界、数量边界、状态流转等需加固
3. **安全审计缺失**：智能体操作、附件访问、异常尝试等缺少审计日志
4. **测试覆盖不足**：并发场景、边界情况、集成测试需补充

建议优先修复 P0 和 P1 问题，补充关键测试用例，然后逐步清理技术债务。

---

**报告生成时间：** 2026-09-10 20:15  
**审查工具：** Claude Opus 5 + 4 个 Claude Sonnet 4-6 子代理  
**审查耗时：** 约 25 分钟

---

## 九、2026-09-11 复核与修复记录

本轮按 P0/P1 顺序对报告中的问题进行了代码核对，并运行了后端全量回归测试（66 passed，1 skipped）及 Ruff 检查（通过）。

### 已确认并修复

- 库存退货、调拨、生产领料和销售过账增加扣减后的非负库存防线。
- 销售退货锁定销售单及明细，并按销售明细汇总请求数量，防止重复明细绕过累计退货校验。
- 附件下载增加存储文件名和真实路径的根目录边界校验，拒绝路径穿越值。
- 采购过账对供应商、仓库和物料活动状态使用行锁读取，避免状态检查与过账之间出现竞态窗口。
- 智能体采购和盘点确认的重复请求保持幂等，同时写入 `CONFIRM_REPLAY` 审计记录。
- 系统健康检查捕获数据库连接异常并返回 503，而不是抛出 500。

### 已核实为当前代码误报或已有保护

- 销售过账已经对库存余额使用 `with_for_update()`，本轮补充了显式非负校验。
- 种植周期重叠条件已使用包含日期边界的 `<=`/`>=` 判断。
- 养殖批次关闭后的存栏变动、健康、称重和生产记录入口均有状态校验。
- ADG 仅在称重间隔天数大于 0 时计算，不存在入栏当天除零异常。
- CSRF 内部智能体路径匹配包含结尾斜杠，不会误豁免 `internal-docs` 等相似路径。

### 仍待逐项处理

- 销售利润/收款并发一致性、审计事务边界、任务完成权限等报告条目需要结合各模块业务规则继续核对。
- 智能体确认令牌目前通过短时签名、用户版本和业务单据版本保护，重复请求保留幂等；若部署要求严格一次性消费，需要引入 Redis 或持久化 nonce 表后再切换。
- 附件资源类型的细粒度角色策略尚未改变，当前沿用“拥有农场访问权即可访问该农场附件”的系统规则。
- 并发压测、覆盖率目标、OpenAPI 文档和生产库迁移仍属于后续质量门禁工作。

## 十、2026-09-12 复核与修复记录

本轮继续核对未关闭的高危/中危条目，并完成后端全量回归测试（66 passed，1 skipped）及 Ruff 检查。

### 本轮已修复

- 收款按农场和收款单号加锁，销售单收款余额更新使用行锁；重复单号内容不一致时拒绝，竞态冲突保持幂等。
- 销售退货拒绝未来日期。
- 采购草稿仅允许创建人修改，农场负责人和系统管理员可以代办。
- 任务完成使用行锁，并限制为任务创建人、农场负责人或系统管理员，避免普通操作员篡改他人任务状态。
- 盘点过账和库存调整在同一业务事务中写入 `POST` 审计日志。
- 附件绑定资源类型和资源编号经过白名单及农场归属校验，拒绝跨农场资源引用。
- 智能体工具角色等级统一使用 `ROLE_LEVELS` 常量，避免权限目录与等级映射分离。

### 本轮已确认无需修改

- Agent API Key 使用固定时序安全比较，所有外部 Agent 路由均经过同一装饰器，未发现可绕过路径。
- 销售单过账、销售退货、种植周期状态、养殖批次关闭和库存盘点均已存在行锁或版本校验。
- 审计日志在任务创建、任务完成和盘点过账事务内提交；智能体确认还会追加确认来源审计。

### 仍保留的发布门禁

- 确认令牌严格“一次性消费”已通过 `0024_agent_confirmation_nonces` 持久化 nonce 表、唯一约束和业务单据行锁实现；过期 nonce 的定期清理属于运维优化。
- 附件当前按“拥有农场访问权”授权，尚未引入按资源类型区分的查看角色策略。
- 并发压测、覆盖率目标、完整 OpenAPI 覆盖、迁移回滚演练和桌面/移动端人工回归仍属于发布前质量门禁。
