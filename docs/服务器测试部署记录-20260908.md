# 服务器测试部署记录（2026-09-08）

## 范围

- 部署目录：服务器上的隔离测试目录（不使用正式应用目录）。
- 测试数据库：`agriculture_management_test`。
- 测试服务：Waitress `127.0.0.1:15000`。
- 正式服务：Waitress `127.0.0.1:5000`，本次未停止、未升级、未改配置。
- 测试代码目标迁移：`0023_attachments`。

## 部署步骤

1. 从服务器备份恢复测试库。
2. 执行 `0011_livestock_production → 0023_attachments` 迁移。
3. 启动测试 Waitress 实例，并保留前端 `dist` 构建产物。
4. 使用隔离测试管理员和演示农场执行 API 冒烟。

## 结果

- `schema-check`：通过。
- `inventory-reconcile`：通过。
- `trade-reconcile`：通过。
- `/api/health`、版本化健康接口、登录会话、农场、基础资料、库存、库存分析、经营看板、销售、任务、审计和附件只读接口：通过。
- 缺少智能体 API Key 时返回预期 `AGENT_API_KEY_NOT_CONFIGURED`，CSRF 缺失时返回预期 `CSRF_INVALID`。
- 测试服务健康接口 20 次请求：P95 约 `2.6ms`（服务器回环地址）。
- 正式服务健康检查仍返回成功，正式 Waitress 进程保持原 PID 运行。

## 夹具修复

`seed-agent-demo` 现在会为演示库存创建幂等的期初库存流水，避免测试数据只有余额没有流水而触发库存对账差异。该改动不涉及正式数据库数据。

## 结论

服务器测试部署和回归通过，可以继续进行发布前人工桌面/移动端回归。正式数据库迁移和正式服务切换仍需单独授权。
